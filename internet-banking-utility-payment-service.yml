server:
  port: 8085

spring:
  datasource:
    url: jdbc:mysql://192.168.1.93:3306/banking_core_utility_payment_service
    username: leo
    password: mysqlleo
  jpa:
    hibernate:
      ddl-auto: update

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8081/eureka

info:
  app:
    name: ${spring.application.name}
